[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
black = "*"
pylint = "*"

[packages]
matplotlib = "*"
pillow = "*"
pydot-ng = "*"
opencv-python = "*"
pandas = "*"
tensorflow = "*"
keras = "*"
jupyterlab = "*"

#pypiwin32 = "*" #  Is required for Windows only, install manually and skip locking dependencies when setting up a venv. 
                 #    To do so, run:   pipenv install
                 #                     pipenv isntall pypiwin32 --skip-lock
                 #                     <path to bash>$ git checkout Pipfile.lock
                 #                                  >$ git checkout Pipfile   
                 #///////////
                 # When setting up a venv on linux, just run:    pipenv install
                 #///////////
                 # Ultimate check: if all steps are completed correctly, then pypiwin32 should not be listed as installed package in this file

[requires]
python_version = "3.8"

[pipenv]
allow_prereleases = true
